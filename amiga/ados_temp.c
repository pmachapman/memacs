/* This file contains empty template routines that
 * the IDCMP handler will call uppon. Fill out these
 * routines with your code or use them as a reference
 * to create your program.
 */

int Project0Item0( void )
{
	/* routine when (sub)item "Open ...     ^X^F" is selected. */
}

int Project0Item1( void )
{
	/* routine when (sub)item "Save         ^X^S" is selected. */
}

int Project0Item2( void )
{
	/* routine when (sub)item "Save as ...  ^X^W" is selected. */
}

int Project0Item3( void )
{
	/* routine when (sub)item "List Buffers ^X^B" is selected. */
}

int Project0Item4( void )
{
	/* routine when (sub)item "Quit         ^X^C" is selected. */
}

int Project0Item5( void )
{
	/* routine when (sub)item "Set Mark     ^@" is selected. */
}

int Project0Item6( void )
{
	/* routine when (sub)item "Cut Region   M-W" is selected. */
}

int Project0Item7( void )
{
	/* routine when (sub)item "Copy Region  M-Y" is selected. */
}

int Project0Item8( void )
{
	/* routine when (sub)item "Transpose    ^T" is selected. */
}

int Project0Item9( void )
{
	/* routine when (sub)item "Quote Char   ^Q" is selected. */
}

int Project0Item10( void )
{
	/* routine when (sub)item "Uppercase Rg ^X^U" is selected. */
}

int Project0Item11( void )
{
	/* routine when (sub)item "Lowercase Rg ^X^L" is selected. */
}

int Project0Item12( void )
{
	/* routine when (sub)item "List         ^X^B" is selected. */
}

int Project0Item13( void )
{
	/* routine when (sub)item "Select       ^Xb" is selected. */
}

int Project0Item14( void )
{
	/* routine when (sub)item "Kill         ^Xk" is selected. */
}

int Project0Item15( void )
{
	/* routine when (sub)item "Insert       M-^Y" is selected. */
}

int Project0Item16( void )
{
	/* routine when (sub)item "Justify      ^XJ" is selected. */
}

int Project0Item17( void )
{
	/* routine when (sub)item "Redisplay    ^L" is selected. */
}

int Project0CloseWindow( void )
{
	/* routine for "IDCMP_CLOSEWINDOW". */
}

